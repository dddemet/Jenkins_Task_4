#!/usr/bin/env groovy

pipeline {
	agent any
    stages {
        stage('BUILD') {
            steps{
                sh "echo TEST"
                sh "ls -lah"
            }
        }
        stage('TEST') {
			steps {
                sh "echo TEST"
            }
	  	}
        stage('PUSH') {
            steps{
                sh "echo TEST"
            }
        }
    }
}
