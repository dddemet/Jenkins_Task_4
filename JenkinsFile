#!/usr/bin/env groovy

pipeline {
	agent any
    stages {
        stage('BUILD') {
            steps{
                sh "docker build -t dddemet777/lab2:\$(date '+%Y-%m-%d-%H-%M') ."
            }
        }
        stage('TEST') {
			steps {
                sh "echo TEST"
            }
  	}
    }
}
