#!/usr/bin/env groovy

pipeline {
	agent any
    stages {
        stage('BUILD') {
            steps{
                sh "TEST"
            }
        }
        stage('TEST') {
			steps {
                sh "TEST"
            }
	  	}
        stage('PUSH') {
            steps{
                sh "TEST"
            }
        }
    }
}
